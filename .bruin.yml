default_environment: us
environments:
    us:
        connections:
            duckdb:
                - name: duckdb-default
                  path: outputs/us/pipeline.duckdb
    fr:
        connections:
            duckdb:
                - name: duckdb-default
                  path: outputs/fr/pipeline.duckdb
    eu:
        connections:
            duckdb:
                - name: duckdb-default
                  path: outputs/eu/pipeline.duckdb
